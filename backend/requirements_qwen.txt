# Requirements for Qwen3-VL Backend
# Use this instead of the original DeepSeek-OCR requirements

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# vLLM for model serving
vllm>=0.11.0

# Transformers (latest version or from source for Qwen3-VL support)
# If stable release doesn't support qwen3_vl_moe yet, install from source:
# pip install git+https://github.com/huggingface/transformers.git
transformers>=4.40.0

# HuggingFace dependencies
huggingface-hub>=0.30.0,<1.0  # Required by transformers
accelerate>=0.20.0
tokenizers>=0.15.0

# Qwen-specific
qwen-vl-utils==0.0.14

# Image processing
Pillow>=10.0.0
PyMuPDF>=1.23.0  # For PDF handling

# Deep learning
torch>=2.0.0
torchvision>=0.15.0

# Optional but recommended
numpy>=1.24.0
python-dotenv>=1.0.0

# Development
pytest>=7.4.0
httpx>=0.25.0  # For testing FastAPI
